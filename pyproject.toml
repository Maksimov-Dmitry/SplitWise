[project]
name = "splitwise"
version = "0.1.0"
description = "Simple expense splitting app"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.41.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "RUF"]
